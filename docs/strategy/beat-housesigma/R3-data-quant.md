# R3 — data-quant (sign-off)

**ENDORSE.**

The merged shortlist is faithful to the data reality I verified live. Confirming the two items the lead flagged as mine:
- **Redline CONFIRMED:** B (kill fake `ExtrapolatedCapRate`, keep honest `capital_burn_rate`) ships in the **same release as A** — never stabilize a cluster that's still serving fabricated cap rates. Captured at lines 11 & 37. ✅
- **D-split CONFIRMED:** D1 (rent model → 3 missing feeder tables, zero new engine code, two-column `rent_idx_public`/`rent_blended_gated` with min-N≥8) + D2 (trust-spine wiring) as one Phase-1 de-fake. H hard-downstream of D1. L is a near-free rider. All captured exactly. ✅

No dissent. One non-blocking note for execution: flipping the first-run default `smart`→`flippers` (B) is the right call — it guarantees the public first impression renders only populated, honest fields (True DOM / price-drop / capital burn), which is the whole point of leading with the wedge while D1 is built behind it.
