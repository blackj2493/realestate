name: Daily Real Estate Sync

# Scheduled daily sync (3:00 AM UTC). The earlier "premature close" failures were a
# node-fetch bug — cross-fetch (→ node-fetch) choked on truncated REST response bodies on
# GitHub Actions' egress to Supabase. Fixed at the root in src/lib/supabase/client.ts by
# wrapping native fetch (undici), which tolerates that path — so this runs reliably on
# GitHub Actions again and the schedule is restored. (A Railway-cron alternative was
# started but never finished; see docs/railway-cron-setup.md. Run only ONE scheduler to
# avoid double-sync.)
on:
  schedule:
    - cron: '0 3 * * *'
  # Also allow manual triggering from the GitHub website / `gh workflow run`.
  workflow_dispatch:

# Least privilege: checkout needs contents:read; the failure notifier opens an issue.
permissions:
  contents: read
  issues: write

jobs:
  run-ingester:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # was '20'; @supabase/realtime-js needs native WebSocket (Node >=22)

      - name: Install Dependencies
        run: npm ci # Uses package-lock.json for a clean install

      - name: Run Daily Sync Script
        # The load-bearing core sync. Measured cost is ~2 min per 100-listing page
        # (per-page /Media + /PropertyRooms enrichment + Supabase upsert + Typesense
        # index), and the active feed churns ~5.5k listings/day → ~120-180 min for a
        # NORMAL daily delta (Query A+B+C+reconcile), more when catching up a multi-day
        # backlog. The old 120m cap was below even a normal run, so the core sync timed
        # out before advancing the cursor — and since the cursor only moves on full
        # success, every retry re-attempted an ever-growing window and timed out again.
        # 300m clears a multi-day backlog and leaves a buffer before GitHub's 6h job
        # ceiling; the downstream refresh steps are continue-on-error and self-heal if
        # squeezed. A genuine hang still exits non-zero (cursor NOT advanced, §12).
        # TODO(perf): parallelize per-page media/rooms enrichment to cut the ~2min/page.
        timeout-minutes: 300
        env:
          # FEED ISOLATION: IDX and VOW are TWO DISTINCT TRREB tokens with no fallback.
          # Read by scripts/worker/ingester.ts:242-243.
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          PROPTX_IDX_TOKEN: ${{ secrets.PROPTX_IDX_TOKEN }}
          PROPTX_VOW_TOKEN: ${{ secrets.PROPTX_VOW_TOKEN }}
          # ingester.ts imports sync.ts, which constructs a Typesense admin client at
          # module load and HARD-THROWS without this key (sync.ts:38). Required for the
          # ETL to index listings into Typesense — not optional.
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
        run: npx tsx scripts/worker/ingester.ts sync

      # Self-heal the sold_listings search collection: re-derive the last 7 days from
      # raw_vow_sold (idempotent upserts). Query B indexes sold rows inline, but a
      # transient per-doc import failure silently drops that row from the dashboard
      # SOLD panel — and until 2026-07-22 the only heal was the Sun/Wed reconcile,
      # leaving quiet multi-day gaps (the East Gwillimbury report). This bounds any
      # inline miss to 24h. Additive — continue-on-error.
      - name: Reindex Sold Window (7d)
        continue-on-error: true
        timeout-minutes: 15
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
        run: npx tsx scripts/worker/soldIndexer.ts backfill --days 7

      # Recover relist SOLD closes the delta crawl (Query B) missed. A terminate-then-relist
      # resell closes under a NEW MLS# that can enter the feed behind the sold watermark, so it
      # never reaches raw_vow_sold — surviving only in property_campaign_history (populated by
      # an unfiltered by-address query). This bounded backstop scans ledger rows refreshed in
      # the last week, re-fetches the missing closes by MLS# from the VOW feed, and re-derives
      # the comp with the exact ingester mappers. Placed BEFORE the raw_vow_sold-derived
      # precomputes below (sale history, region/market aggregates, estimates) so they pick up
      # the recovered comps the same night. Needs the VOW token (by-key re-fetch) + Typesense
      # (sold_listings import). Idempotent upserts — additive, continue-on-error. The one-time
      # historical backfill is a manual --scan --apply --archive run (see the script header).
      - name: Reconcile Relist Sold Closes (7d)
        continue-on-error: true
        timeout-minutes: 20
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
          PROPTX_VOW_TOKEN: ${{ secrets.PROPTX_VOW_TOKEN }}
        run: npx tsx scripts/admin/reconcile-sold-from-campaign-history.ts --scan --apply --since-days=7 --window-days=180

      # Drop de-listed comps whose property has since SOLD. `pruneOldDelisted` only ever
      # expires de-listed docs on AGE, so a terminate-then-relist resale left the ORIGINAL
      # key lit on the terminal's De-listed layer for up to 90 days after the sale — while
      # /properties/<original key> already rendered SOLD via getListingDetail's relist
      # reconciliation (the 210 Charlton Ave E report; 7,103 of 59,730 For-Sale de-listed
      # pins were stale when measured 2026-08-03). Both indexers now handle this inline —
      # soldIndexer purges on close, delistedIndexer withholds a replayed de-list — so this
      # whole-window join is only a backstop for anything they missed (e.g. a close indexed
      # while Typesense was refusing the purge). A LEASE deliberately does NOT retire a pin:
      # the owner still owns the property and still wanted to sell (see SUPERSEDING_DEAL_TYPES).
      # Placed AFTER every step that writes a close so it sees tonight's full picture.
      # Idempotent; typically deletes 0. Additive — continue-on-error.
      - name: Sweep Superseded De-listed Comps
        continue-on-error: true
        timeout-minutes: 15
        env:
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
        run: npx tsx scripts/worker/purgeSupersededDelisted.ts sweep --apply

      # Re-floor active True DOM to the naive current-listing age. The floor that keeps
      # true_dom honest — GREATEST(true_dom, days-since-OriginalEntryTimestamp) — lives only
      # in the core sync's processBatch, which runs on the ModificationTimestamp DELTA. A
      # listing that is stable in the feed AND has no campaign-history ledger row falls out of
      # every delta and freezes at true_dom=0 as it ages (2026-08-02: 22k active rows >60d old
      # reading 0). The region RPCs mask this with a query-time floor, but Typesense/the flat
      # column do not, so per-listing cards / watchlist / IsStale bubbles under-report DOM and
      # drop STALE badges. This persists the same floor to the flat column AND pushes
      # TrueDom/IsStale to the `properties` collection. --min-drift=3 bounds nightly churn
      # (stable cards within 3 days; a 60d stale-line crossing is always caught immediately).
      # Placed before the region/market precomputes so the flat column is fresh (they floor at
      # query time regardless). Raw pg → Session-pooler DATABASE_URL + Typesense admin key.
      # Additive — must never break the core sync, so continue-on-error. The one-time full
      # backfill is a manual `--apply` (min-drift 1) run; see the script header.
      - name: Re-floor Active True DOM
        continue-on-error: true
        timeout-minutes: 30
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
        run: npx tsx scripts/admin/refloor-active-true-dom.ts --apply --min-drift=3

      # Refresh precomputed condo fee/sqft aggregates from the freshly-synced VOW data.
      # Reads raw_vow_sold READ-ONLY and writes the separate condo_fee_stats table; needs
      # only the Supabase credentials (no TRREB tokens). Keeps the Fee Stability card ≤24h fresh.
      - name: Refresh Condo Fee Stats
        # Additive aggregation for a single listing-page card — must never break the
        # core daily sync. timeout-minutes bounds it so a hang here can't cascade into
        # skipping the revalidate step at the end (the 2026-06-16→24 outage root cause).
        continue-on-error: true
        timeout-minutes: 20
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-condo-fee-stats.ts --apply

      # Closed-rent aggregates for the public /data/rents tracker (migration 119). One
      # server-side GROUPING SETS statement over raw_vow_sold lease rows — measured ~2s for
      # the whole table, so the 10-minute bound is generous. Uses DATABASE_URL (pg) rather
      # than the Supabase REST creds precisely because the aggregation happens in Postgres:
      # nothing crosses the wire but finished cells. Additive — must never break the sync.
      - name: Refresh Rent Market Stats
        continue-on-error: true
        timeout-minutes: 10
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/admin/refresh-rent-market-stats.ts --apply

      # Sold-vs-asking outcomes for the public /data/over-asking tracker (migration 120). Same
      # shape as the rent job above: one server-side GROUPING SETS statement over raw_vow_sold
      # sale rows, ~2s for the whole table, so the 10-minute bound is generous. Without this
      # step the tracker would freeze at whatever the backfill wrote and go on serving it with
      # a "updated nightly" stamp — the failure mode this codebase keeps rediscovering.
      # Additive — must never break the sync.
      - name: Refresh Sale Competition Stats
        continue-on-error: true
        timeout-minutes: 10
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/admin/refresh-sale-competition-stats.ts --apply

      # Precompute base-scope market aggregates (cap rate, active/stale counts, 24mo sold
      # trend) for every curated market into market_summary (migration 048), so the
      # Submarket Leaderboard / Region Scorecard read ONE ready-made row instead of running
      # the ~43s region_*_aggregates RPCs live on the request path (Toronto exceeded the 30s
      # Supabase fetch timeout, hanging the Market Trends page). Sequential per market — never
      # the old 15-way fan-out that hit the 60s statement_timeout. Reads listings +
      # raw_vow_sold, writes the separate market_summary table; Supabase creds only. Additive
      # — must never break the core sync, so continue-on-error.
      - name: Refresh Market Summary
        continue-on-error: true
        timeout-minutes: 30
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-market-summary.ts --apply

      # Precompute the FULL base-scope /analytics payload (all 11 metrics per curated
      # market) into region_metrics (migration 081), so the default Market Trends view and
      # the dashboard scorecard read one point lookup instead of the live RPC batch.
      # NOTE: this step (and Capture Price Events below) previously existed ONLY in the
      # abandoned Railway orchestrator (scripts/worker/dailySync.ts) and was never wired
      # into this workflow — the 2026-07-22 canary freshness alert. Reads the same tables
      # as the market-summary step; Supabase creds only. Additive — continue-on-error.
      - name: Refresh Region Metrics
        continue-on-error: true
        timeout-minutes: 30
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-region-metrics.ts --apply

      # Multi-cut price ledger (migration 069): diff each active listing's fresh
      # list_price against listing_price_state, append price_events on a change. Never
      # scheduled anywhere until 2026-07-22 — price_events had ZERO rows in prod (the
      # canary's price-ledger check now watches for exactly this). Uses raw pg, so it
      # needs the Session-pooler DATABASE_URL secret (like the geo step), NOT the REST
      # creds. Additive — continue-on-error.
      - name: Capture Price Events
        continue-on-error: true
        timeout-minutes: 20
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/admin/capture-price-events.ts --apply

      # Refresh the precomputed prior-sale ledger from the freshly-synced VOW data.
      # Reads raw_vow_sold READ-ONLY and writes the separate property_sale_history table;
      # needs only the Supabase credentials (no TRREB tokens). Powers the listing-page
      # Sale History + Price Timeline sold markers. Additive — must never break the core
      # daily sync, so continue-on-error.
      - name: Refresh Property Sale History
        continue-on-error: true
        timeout-minutes: 20
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-property-sale-history.ts --apply

      # Refresh the AVM sqft-calibration table (median grossed room-sum GLA per
      # CityRegion + sub-type + LivingAreaRange bucket) from active listings' room
      # dimensions. It's the AVM's no-rooms fallback so a coarse MLS range can't
      # drive the estimate. Reads listings only, writes the separate
      # avm_sqft_calibration table; Supabase creds only. Additive — must never
      # break the core sync, so continue-on-error.
      - name: Refresh AVM Sqft Calibration
        continue-on-error: true
        timeout-minutes: 20
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-sqft-calibration.ts --apply

      # Refresh the AVM trend index + community offset (migration 024) from the
      # freshly-synced VOW data. Splits price into city × sub-type half-year trend
      # and community vs city offset so the AVM anchor can be both LOCAL in level
      # and CURRENT in trend, even when 0 recent local comps exist. Reads
      # raw_vow_sold READ-ONLY (scalar columns only — respects the Disk IO budget)
      # and writes the separate avm_trend_index / avm_community_offset tables;
      # Supabase creds only. Additive — must never break the core sync, so
      # continue-on-error.
      - name: Refresh AVM Trend + Offset
        continue-on-error: true
        timeout-minutes: 15
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-avm-trend-offset.ts --apply

      # Precompute the PureProperty Estimate (AVM) + resolved GLA/$psf per active
      # listing into property_estimates, so the Compare page reads a ready-made
      # estimate in one batched lookup instead of running the multi-query AVM live.
      # Runs AFTER the sqft-calibration step so it can use the fresh no-rooms
      # fallback; reads listings + raw_vow_sold (anchor) READ-ONLY, writes the
      # separate property_estimates table; Supabase creds only. Additive — must
      # never break the core sync, so continue-on-error.
      #
      # DELTA (--since ~25h): only re-estimate listings re-synced today. The full-table
      # form scanned ~77k active listings nightly (~4.5h) and blew the 6h job ceiling —
      # which silently SKIPPED the "Revalidate Synced Listings" step below, so price
      # changes stopped reaching the listing pages (outage 2026-06-16→24). A listing's
      # estimate inputs only change when the sync re-ingests it, so the delta pass keeps
      # the same rows fresh in minutes. Run the bare --apply (full table) form via
      # workflow_dispatch periodically to re-base every row against the latest anchors.
      - name: Refresh Property Estimates
        continue-on-error: true
        timeout-minutes: 30
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/admin/refresh-property-estimates.ts --apply --since "$(date -u -d '25 hours ago' +%Y-%m-%dT%H:%M:%SZ)"

      # Prune orphaned estimates. property_estimates keeps ONE row per ACTIVE listing, but the
      # refresh only WRITES active rows — a listing that has since sold/terminated/dropped out
      # is skipped, never deleted, so its estimate lingers forever (frozen computed_at), bloats
      # the table (Disk IO budget §12), and the freshness canary miscounts it as a stale ACTIVE
      # estimate (2026-08-02: 24.5k such orphans). Deletes rows that are BOTH stale (>120h) AND
      # not active-eligible by the SAME isActive predicate the refresh uses — active rows are
      # never touched, and every estimate is regenerable, so it's non-destructive. Raw pg →
      # Session-pooler DATABASE_URL (like Capture Price Events). Additive — continue-on-error.
      - name: Prune Orphaned Estimates
        continue-on-error: true
        timeout-minutes: 15
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/admin/prune-property-estimates.ts --apply

      # Precompute geo "Things to Know" flags (flood now; rail/traffic later) for
      # listings synced in the last day, into listing_geo_flags. PUBLIC-records
      # PostGIS spatial joins — reads listings, writes the separate listing_geo_flags
      # table. Unlike the refresh steps above it uses raw pg, so it needs the
      # Session-pooler DATABASE_URL secret (CLAUDE.md §12), NOT the Supabase REST
      # creds. Delta window (~25h) covers the daily cadence with margin. Additive —
      # must never break the core sync, so continue-on-error; the step errors out
      # cleanly (and is skipped) if the DATABASE_URL secret is unset.
      - name: Enrich Geo Things-to-Know Flags
        continue-on-error: true
        timeout-minutes: 25
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/worker/enrichGeoFlags.ts --since "$(date -u -d '25 hours ago' +%Y-%m-%dT%H:%M:%SZ)"

      # VOW-only ACTIVE listings — the ~16.4k homes the VOW datafeed carries that IDX
      # does not (measured 2026-08-09: 112,680 vs 96,275 Active). Writes ONLY to
      # vow_active_listings, which is RLS-on-with-no-policies, so nothing ingested here
      # can reach an anonymous surface even by mistake.
      # SELF-SKIPS unless VOW_ACTIVES_ENABLED is "true": adding this step does NOT by
      # itself start ingesting VOW data. The repo variable is the switch, and it should
      # stay off until the PROPTX URL registration (audit R7) covers the production host.
      # Additive — continue-on-error so it can never break the core sync.
      - name: Sync VOW-only Active Listings
        continue-on-error: true
        timeout-minutes: 30
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          PROPTX_VOW_TOKEN: ${{ secrets.PROPTX_VOW_TOKEN }}
          VOW_ACTIVES_ENABLED: ${{ vars.VOW_ACTIVES_ENABLED }}
        run: npx tsx scripts/worker/vowActiveSync.ts

      # Email each user a digest of price drops on their watched properties, using
      # the freshly-synced Typesense index. Additive + must never break the core
      # sync, so continue-on-error. No-ops cleanly if RESEND_API_KEY is unset.
      - name: Send Watchlist Alerts
        continue-on-error: true
        timeout-minutes: 15
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TYPESENSE_ADMIN_API_KEY: ${{ secrets.TYPESENSE_ADMIN_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          ALERTS_FROM_EMAIL: ${{ secrets.ALERTS_FROM_EMAIL }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
        run: npx tsx scripts/worker/alerts.ts

      # Onboarding drip — the milestone-gated welcome sequence (engagement plan WS1.2).
      # Additive + continue-on-error so it can never break the core sync. Fail-closed:
      # no-ops if RESEND_API_KEY is unset AND self-skips until migration 105
      # (user_email_lifecycle) is applied, so it can't re-send without idempotency.
      # Needs the search-only Typesense key for 2B's live example dashboard.
      - name: Send Onboarding Sequence
        continue-on-error: true
        timeout-minutes: 15
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_TYPESENSE_SEARCH_ONLY_API_KEY: ${{ secrets.NEXT_PUBLIC_TYPESENSE_SEARCH_ONLY_API_KEY }}
        run: npx tsx scripts/worker/onboarding.ts

      # Refresh cached listing detail for everything synced today so nightly changes
      # appear immediately instead of within the page's 1h TTL. Additive — must never
      # break the core sync, so continue-on-error; no-ops cleanly if REVALIDATE_SECRET
      # is unset. Runs last: it only reads listings.synced_at, set by the sync above.
      # The per-step timeouts above sum to well under the 6h job ceiling, so this step
      # is now GUARANTEED to be reached even if every preceding refresh step times out.
      - name: Revalidate Synced Listings
        continue-on-error: true
        timeout-minutes: 20
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          REVALIDATE_SECRET: ${{ secrets.REVALIDATE_SECRET }}
        run: npx tsx scripts/worker/revalidateListings.ts

      # Retention pruning for the VOW consumer-access audit trail (migration 053 / §5.4).
      # Additive + continue-on-error: a prune failure must never affect the sync. Idempotent
      # (deletes rows older than the retention window; most days delete nothing).
      - name: Prune VOW Access Log
        continue-on-error: true
        timeout-minutes: 10
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: npx tsx scripts/worker/pruneVowAccessLog.ts

  # ── Failure notifier ────────────────────────────────────────────────────────
  # Fires ONLY when run-ingester fails — i.e. the core sync step exits non-zero
  # (ingester.ts now exits 1 on a failed sync) or hits its timeout. The additive
  # refresh steps are continue-on-error, so they never trip this; this alerts on a
  # REAL sync failure only. Two channels: a GitHub issue (comment-not-spam dedupe)
  # AND a direct Resend email to SYNC_ALERT_EMAIL — issue notifications are easy
  # to miss, and the operator asked to ALWAYS know when the nightly run fails.
  notify-failure:
    needs: run-ingester
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Open / update sync-failure issue
        uses: actions/github-script@v7
        with:
          script: |
            const label = 'sync-failure';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = [
              `🔴 **${context.workflow}** failed.`,
              ``,
              `- Run: ${runUrl}`,
              `- Commit: \`${context.sha.slice(0, 7)}\``,
              `- Trigger: ${context.eventName}`,
              ``,
              `The core sync exited non-zero (or timed out). The sync_state cursor is preserved on failure, so the next run re-attempts the same window.`,
              ``,
              `cc @${context.repo.owner}`,
            ].join('\n');
            const open = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo, state: 'open', labels: label,
            });
            if (open.data.length > 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: open.data[0].number, body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner, repo: context.repo.repo,
                title: `🔴 Daily sync failed`, body, labels: [label],
              });
            }

      # Direct email as well — the same Resend sender the freshness jobs use
      # (notifyRun.ts convention), but dependency-free: this job has no checkout /
      # npm ci, so a plain curl keeps it instant. No-ops cleanly if secrets are unset;
      # never fails the job (the red run already stands on its own).
      - name: Email failure alert
        if: always()
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          SYNC_ALERT_EMAIL: ${{ secrets.SYNC_ALERT_EMAIL }}
          ALERTS_FROM_EMAIL: ${{ secrets.ALERTS_FROM_EMAIL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [ -z "$RESEND_API_KEY" ] || [ -z "$SYNC_ALERT_EMAIL" ]; then
            echo "(no email sent — set RESEND_API_KEY + SYNC_ALERT_EMAIL secrets)"; exit 0
          fi
          FROM="${ALERTS_FROM_EMAIL:-PureProperty Alerts <support@pureproperty.ca>}"
          jq -n --arg from "$FROM" --arg to "$SYNC_ALERT_EMAIL" \
                --arg subject "❌ PureProperty · Daily sync FAILED" \
                --arg html "<p>🔴 The <b>daily sync</b> failed — the core ingester exited non-zero or timed out.</p><p><a href=\"${RUN_URL}\">View the run</a>. The sync cursors are preserved on failure, so the next run re-attempts the same window.</p>" \
                '{from: $from, to: $to, subject: $subject, html: $html}' \
            | curl -sS -X POST https://api.resend.com/emails \
                -H "Authorization: Bearer $RESEND_API_KEY" \
                -H "Content-Type: application/json" \
                -d @- \
            && echo "" && echo "📧 Emailed $SYNC_ALERT_EMAIL" \
            || echo "email send failed (non-fatal)"