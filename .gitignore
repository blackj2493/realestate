# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Downloaded source data caches (e.g. Ontario SIF XLSX for build-schools-dataset)
.cache/

# Phase 2 geo "Things to Know" — downloaded/converted rail+transit source data.
# Reproduce via docs/geo-data-sources.md; never commit the raw/large data.
data/_geo_src/
data/orwn_track.geojson
data/transit_stations.geojson

# Production build
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local development
.DS_Store
*.pem

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
coverage/

# Misc
*.log

# Reindexer resume checkpoint (runtime state)
scripts/.reindex-cursor

# Admin backfill resume cursors (runtime state)
scripts/admin/.backfill-*-cursor.json

# Superpowers brainstorming companion artifacts
.superpowers/

# Backtest / experiment analysis outputs (large, throwaway — reproduce via scripts/admin)
/avm-*.json
.qa-run/

# Phase-Zero min-n study run output (reproduce via scripts/admin/phaseZeroMinN.ts)
/phase-zero-minn-*.json

# QA smoke-audit scratch (page snapshots + smoke logs — regenerated, not source)
audit/
