# Active Context

## Current Phase
We are currently setting up the foundational backend infrastructure for the "Shadow MLS."

## Current Task
We need to build the Node.js ETL Worker data sanitization module. Specifically, we need to create utility functions that can parse messy real estate JSON payloads (like the VOW and IDX API responses) into clean, structured data before it gets pushed to Supabase and Typesense.

